# OSD уведомления (On-Screen Display)

**Статус:** Планируется  
**Приоритет:** Средний  
**Дата добавления:** 01.12.2025

---

## Описание

Использование встроенных возможностей VLC для отображения текстовых уведомлений поверх видео через **marquee filter**. Это позволит показывать пользователю полезную информацию прямо на экране без прерывания воспроизведения.

---

## Возможности VLC Marquee Filter

**Основные параметры:**
- `--sub-filter marq` - включение фильтра
- `--marq-marquee "text"` - текст для отображения
- `--marq-position N` - позиция на экране (8=низ, 5=верх-слева и т.д.)
- `--marq-color N` - цвет (16777215=белый, 65280=зелёный, 16711680=красный)
- `--marq-size N` - размер шрифта
- `--marq-timeout N` - автоскрытие в миллисекундах

**Динамическое управление через RC:**
```bash
echo "marq-marquee: Новый текст" | nc localhost 4212
echo "marq-timeout: 5000" | nc localhost 4212
```

---

## Предлагаемое использование

### 1. Информация об эпизоде при запуске

При старте видео показывать:
- Название сериала
- Номер эпизода
- Версия (HDR, 1080p, и т.д.)

**Пример:**
```
┌─────────────────────────────┐
│ Euphoria S01E03 - 1080p     │
└─────────────────────────────┘
```

**Время отображения:** 4-5 секунд

### 2. Уведомление об альтернативных версиях

При обнаружении другой версии сериала (через `db_find_other_versions()`):

**Пример:**
```
┌──────────────────────────────────────────┐
│ ⚠️ Найдена другая версия:                │
│ HDR.2160p - просмотрено 16%              │
└──────────────────────────────────────────┘
```

**Цвет:** Жёлтый (предупреждение)  
**Время:** 6-8 секунд

### 3. Прогресс загрузки/буферизации

Показывать статус при долгой загрузке:

**Пример:**
```
Загрузка... 45%
```

### 4. Защита от засыпания (будущая фича)

Обратный отсчёт перед автопродолжением:

**Пример:**
```
┌─────────────────────────────────┐
│ Следующий эпизод: S01E04        │
│ Нажмите любую кнопку (25 сек)   │
└─────────────────────────────────┘
```

**Цвет:** Красный (требует внимания)  
**Обновление:** Каждую секунду

### 5. Пропуск заставок/титров

Уведомление о пропуске:

**Пример:**
```
⏩ Заставка пропущена
```

**Время:** 2-3 секунды

---

## Техническая реализация

### Новый модуль: `vlc-osd.sh`

```bash
#!/bin/bash

# vlc-osd.sh - Библиотека для OSD уведомлений

VLC_RC_PORT=4212

# Показать OSD сообщение
# $1 - текст, $2 - timeout (мс), $3 - цвет (опционально)
show_osd() {
    local message="$1"
    local timeout="${2:-3000}"
    local color="${3:-16777215}"  # По умолчанию белый
    
    echo "marq-marquee: $message" | nc -w 1 localhost $VLC_RC_PORT 2>/dev/null
    echo "marq-timeout: $timeout" | nc -w 1 localhost $VLC_RC_PORT 2>/dev/null
    echo "marq-color: $color" | nc -w 1 localhost $VLC_RC_PORT 2>/dev/null
}

# Скрыть OSD
hide_osd() {
    echo "marq-marquee: " | nc -w 1 localhost $VLC_RC_PORT 2>/dev/null
}

# Предустановленные типы сообщений
show_info() {
    show_osd "$1" "${2:-4000}" 16777215  # Белый
}

show_warning() {
    show_osd "⚠️ $1" "${2:-6000}" 16776960  # Жёлтый
}

show_error() {
    show_osd "❌ $1" "${2:-8000}" 16711680  # Красный
}

show_success() {
    show_osd "✅ $1" "${2:-3000}" 65280  # Зелёный
}
```

### Интеграция в vlc-cec.sh

```bash
# Подключить библиотеку
source "$SCRIPT_DIR/vlc-osd.sh"

# При запуске видео
local filename=$(basename "$VIDEO_FILE")
local series_prefix=$(extract_series_prefix "$filename")
local series_suffix=$(extract_series_suffix "$filename")

if [ -n "$series_prefix" ]; then
    # Определить номер эпизода
    local episode=$(echo "$filename" | grep -oE "E[0-9]{2}")
    
    # Показать информацию
    show_info "$series_prefix $episode - $series_suffix" 5000
    
    # Проверить альтернативные версии
    local other=$(db_find_other_versions "$series_prefix" "$series_suffix")
    if [ -n "$other" ]; then
        # Парсим первую найденную версию
        IFS='|' read -r alt_suffix alt_file alt_percent <<< "$other"
        show_warning "Найдена версия: $alt_suffix ($alt_percent%)" 7000
    fi
fi
```

### Запуск VLC с marquee

**Изменить параметры запуска в vlc-cec.sh:**

```bash
cvlc "$VIDEO_FILE" \
    --extraintf rc \
    --rc-host localhost:4212 \
    --sub-filter marq \
    --marq-marquee "" \
    --marq-position 8 \
    --marq-size 28 \
    --fullscreen \
    # ... остальные параметры
```

---

## UI/UX Guidelines

### Позиционирование

**Рекомендуемые позиции:**
- `8` (низ центр) - основная информация об эпизоде
- `10` (низ справа) - прогресс, счётчики
- `5` (верх слева) - предупреждения, ошибки

### Цветовая схема

| Тип сообщения | Цвет | RGB | Decimal |
|---------------|------|-----|---------|
| Информация | Белый | (255,255,255) | 16777215 |
| Успех | Зелёный | (0,255,0) | 65280 |
| Предупреждение | Жёлтый | (255,255,0) | 16776960 |
| Ошибка | Красный | (255,0,0) | 16711680 |

### Время отображения

- **Быстрые:** 2-3 сек (пропуск заставки)
- **Обычные:** 4-5 сек (информация об эпизоде)
- **Важные:** 6-8 сек (альтернативные версии)
- **Критичные:** 10+ сек (ожидание действия пользователя)

### Размер шрифта

- **Маленький:** 20-24 (дополнительная информация)
- **Средний:** 28-32 (основные сообщения)
- **Большой:** 36-40 (важные уведомления)

---

## Примеры использования

### Сценарий 1: Запуск эпизода с OSD

```bash
# 1. Запуск VLC с marquee
vlc_start_with_osd "$VIDEO_FILE"

# 2. Показать информацию об эпизоде
show_info "Euphoria S01E03 - 1080p" 5000

# 3. Через 5 секунд проверить альтернативы
sleep 5
check_and_notify_alternatives "$series_prefix" "$series_suffix"
```

### Сценарий 2: Уведомление о пропуске

```bash
# При обнаружении заставки
if should_skip_intro; then
    show_success "Заставка пропущена" 3000
    seek_to $intro_end
fi
```

### Сценарий 3: Защита от засыпания

```bash
# Перед автопродолжением
for i in {30..1}; do
    show_warning "Следующий эпизод через $i сек" 1000
    sleep 1
    
    # Проверка нажатия кнопки
    if user_pressed_key; then
        show_success "Продолжаем!" 2000
        break
    fi
done
```

---

## Преимущества

✅ **Встроенная функциональность VLC** - не требует дополнительных зависимостей  
✅ **Динамическое управление** - можно менять текст во время воспроизведения  
✅ **Настраиваемый вид** - цвет, размер, позиция, прозрачность  
✅ **Автоматическое скрытие** - через timeout  
✅ **Не прерывает воспроизведение** - текст поверх видео

---

## Ограничения

❌ Только простой текст (нет изображений/иконок)  
❌ Нет сложного форматирования (жирный/курсив)  
❌ Нет интерактивных элементов (кнопки)  
❌ Ограниченная кириллица (зависит от шрифтов системы)

---

## Приоритет реализации

**Рекомендуемая последовательность:**
1. ✅ Базовая интеграция SQLite
2. ✅ Композитный ключ series_key
3. 🔜 **OSD уведомления** ← можно добавить сразу
4. 🔜 Автопродолжение сериалов
5. 🔜 Skip intro/outro с OSD уведомлениями
6. 🔜 Защита от засыпания с OSD обратным отсчётом

---

## Документация

Полное руководство по VLC OSD: [`DOCS/reference/VLC_OSD.md`](file:///Users/babayka/PycharmProjects/vlc/DOCS/reference/VLC_OSD.md)

---

## Примечания

- OSD можно добавить независимо от других фич
- Минимальные изменения в существующем коде
- Улучшает UX без изменения основной логики
- Хорошо комбинируется с будущими фичами (Sleep Protection, Skip Intro)
