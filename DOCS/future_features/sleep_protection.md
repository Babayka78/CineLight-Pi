# Защита от засыпания (Sleep Protection)

**Статус:** Планируется  
**Приоритет:** Средний  
**Дата добавления:** 01.12.2025

---

## Описание проблемы

При автовоспроизведении сериалов пользователь может заснуть во время просмотра. В результате:
- Сериал продолжает воспроизводиться без участия пользователя
- Теряется информация о том, на каком эпизоде пользователь реально закончил просмотр
- Приходится вручную искать последний просмотренный эпизод

---

## Предлагаемое решение

### Функция "Защита от засыпания"

**Принцип работы:**
1. Активируется только при включенной опции **Auto Continue** (автовоспроизведение)
2. После завершения каждого эпизода, **перед** запуском следующего:
   - Показывается диалог с запросом подтверждения
   - Пользователю даётся время (например, 30 секунд) для нажатия любой кнопки
3. **Если кнопка нажата:**
   - Продолжается воспроизведение следующего эпизода
4. **Если таймаут истёк (кнопка не нажата):**
   - Последний просмотренный эпизод помечается специальным статусом `[S]` (Sleeping)
   - Автовоспроизведение останавливается
   - Возврат в главное меню

### Настройка

Новая опция в настройках сериала:
```
[ ] Auto Continue
[ ] Skip Intro
[ ] Skip Outro
[ ] Sleep Protection  ← новая опция
```

**Зависимость:** Sleep Protection доступна только если включен Auto Continue.

---

## Технические детали

### Статус `[S]` (Sleeping)

**Отличия от других статусов:**
- `[ ]` - не просмотрено (percent = 0)
- `[T]` - частично просмотрено (0 < percent < 95)
- `[X]` - полностью просмотрено (percent ≥ 95)
- `[S]` - **засыпание** (percent ≥ 95 + sleep_flag = true)

**Сохранение в БД:**

Вариант 1 - новое поле в таблице `playback`:
```sql
ALTER TABLE playback ADD COLUMN sleep_flag BOOLEAN DEFAULT 0;
```

Вариант 2 - использовать существующее поле `description` (временно):
```sql
UPDATE playback SET description = 'SLEEP_DETECTED' WHERE filename = '...';
```

### Диалог подтверждения

**Формат:**
```
┌─────────────────────────────────────────┐
│  Продолжить просмотр?                   │
│                                         │
│  Следующий эпизод:                      │
│  S01E04 - Episode Title                 │
│                                         │
│  Нажмите любую кнопку для продолжения   │
│  (осталось: 25 сек)                     │
└─────────────────────────────────────────┘
```

**Реализация:**
- Использовать `dialog --pause` с таймаутом
- Или CEC wait-for-keypress с таймаутом

### Интеграция

**Файлы для изменения:**
1. `serials.sh` - добавить опцию Sleep Protection в UI
2. `db-manager.sh` - добавить поле `sleep_flag` в схему
3. `playback-tracker.sh` - логика определения статуса `[S]`
4. `video-menu.sh` - отображение статуса `[S]` красным цветом
5. Новый модуль `sleep-detector.sh` - логика диалога и ожидания

---

## User Stories

**As a user:**
- Я хочу безопасно засыпать во время просмотра сериала
- Я хочу легко найти эпизод, на котором я заснул
- Я не хочу терять прогресс просмотра

**As a developer:**
- Система должна корректно определять момент засыпания
- Статус `[S]` должен быть легко различим в меню
- Настройка должна быть опциональной

---

## UI/UX

### Меню сериалов

```
[X] S01E01 - Episode 1
[X] S01E02 - Episode 2
[S] S01E03 - Episode 3  ← красный статус [S]
[ ] S01E04 - Episode 4
[ ] S01E05 - Episode 5
```

### Цветовая схема dialog

- `[` и `]` - синий цвет
- `X`, `T`, пробел - красный цвет
- `S` - **жёлтый или мигающий красный** (особый статус)

---

## Альтернативные варианты

### Вариант 1: Автоматическое определение по времени

- Отслеживать активность пользователя (нажатия кнопок)
- Если нет активности N минут → пометить как `[S]`
- **Минусы:** сложнее реализация, может быть false positive

### Вариант 2: Вопрос в середине эпизода

- Показывать диалог не между эпизодами, а в середине
- **Минусы:** прерывает просмотр, может раздражать

### Вариант 3: Прогрессивный таймаут

- Первый эпизод: 60 секунд ожидания
- Второй эпизод: 45 секунд
- Третий эпизод: 30 секунд
- И так далее
- **Плюсы:** адаптируется под усталость пользователя

---

## Совместимость

**Требования:**
- CEC должен поддерживать ожидание нажатия кнопки
- Dialog должен поддерживать таймауты

**Зависимости:**
- Функция Auto Continue должна быть реализована
- БД должна поддерживать дополнительные флаги

---

## Приоритет реализации

**Рекомендуемая последовательность:**
1. ✅ Завершить миграцию на SQLite БД
2. ✅ Реализовать Auto Continue для сериалов
3. ✅ Реализовать Skip Intro/Outro
4. 🔜 **Защита от засыпания** ← после базовых функций сериалов
5. Ambilight интеграция

---

## Примечания

- Функция особенно полезна для длинных сериалов (10+ эпизодов)
- Может снизить нагрузку на систему (не воспроизводит лишние эпизоды)
- Помогает экономить электроэнергию
