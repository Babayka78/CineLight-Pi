# 🚀 Быстрый старт новой сессии

**Что писать в первом сообщении агенту:**

```
Привет! Продолжаем работу над VLC проектом.

Прочитай:
1. DOCS/INSTRUCTIONS/HANDOFF.md - текущий статус
2. DOCS/CHANGELOG.md - последние изменения

Текущая задача: [описать что нужно сделать]
```

---

## 📋 Если агент забыл контекст:

**Напомни структуру:**
```
Проект: VLC Media Player для Raspberry Pi
- Язык: Bash
- UI: Dialog (TUI)
- Управление: CEC пульт ТВ
- БД: SQLite (в процессе миграции)

Документация:
- DOCS/INSTRUCTIONS/ - рабочие инструкции (для агента)
- DOCS/reference/ - публичная документация
```

---

## ⚡ Краткий чеклист агента:

**Агент должен в начале сессии:**
1. ✅ Прочитать `DOCS/INSTRUCTIONS/HANDOFF.md` (ПОЛНОСТЬЮ)
2. ✅ Посмотреть раздел "Незавершённые задачи"
3. ✅ Проверить токены (должно быть > 150,000)
4. ✅ НЕ читать CHANGELOG автоматически (только по запросу)

---

## 🎯 Приоритетные задачи (сейчас):

**1. Критично - SQLite миграция:**
- Исправить `extract_series_key()` в `db-manager.sh`
- Адаптировать `serials.sh` для БД
- Протестировать интеграцию

**2. Высокий приоритет:**
- Исправить CEC кнопки (RED, GREEN)

**3. Средний приоритет:**
- Запрос о продолжении просмотра (при позиции > 5%)
- Оптимизация автосохранения

---

## 💡 Полезные команды:

```bash
# Просмотр текущего статуса
git status

# Последние изменения
git log --oneline -5

# Токены агента
# Агент сам показывает остаток
```

---

**Этого достаточно для быстрого старта!** 🚀
